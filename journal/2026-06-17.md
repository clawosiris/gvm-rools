# 2026-06-17 — Mainline Landed the Typed Auth Migration and CI Hardening Early, Then Settled into Maintenance and Backlog Pressure

## Mainline & Merged Work

### The repo's post-2026-03-30 movement came in two distinct waves
- The first wave landed on 2026-03-31, when `main` absorbed PR #27 (migrate the CLI auth path to typed GMP command and response handling) and PR #30 (remove obsolete release workflows and add release documentation)
- The second wave arrived on 2026-05-07 and 2026-05-25, when `main` picked up PR #47 (`rand` bump), #52 (GitHub Actions bumps), #53 (cargo dependency group bumps), #54 (quick-xml 0.39.2), #58 (README refresh), #59 (run the security workflow on all pull requests), and #63 (fix `repository_dispatch` payload shape)
- That sequence tells a clear story: the repo did its meaningful product-facing work first by moving the auth path onto the typed GMP surface, then spent the rest of the window on release cleanup, dependency maintenance, and CI hardening rather than new feature expansion

## Issues
- No new issues opened after the 2026-03-30 baseline, and no issues closed in the same window
- That means the repo's visible churn stayed almost entirely in merged maintenance work and PR backlog management rather than new bug reporting

## Open Issues
- [#10](https://github.com/clawosiris/gvm-rools/issues/10) remains the only open tracked issue on `main`, still carrying the low-severity code-review follow-up list from March
- The absence of newer issue churn is not the same thing as a quiet queue. It mostly means unresolved work is accumulating in PRs and failing automation rather than being captured as new issue objects

## CI Health
- `CI` stayed healthier than `Security` in the recent window. Journal PRs and Dependabot PRs repeatedly cleared `CI`, but `Security` kept failing on journal branches, dependency branches, and the 2026-06-15 scheduled run on `main`
- The repo also shows persistent automation pressure rather than a clean queue: open PRs now include a long journal backlog through PR #105 plus fresh dependency branches #103 and #104
- In practical terms, the branch is still moving, but the dominant signal is maintenance drag and security-workflow instability rather than new CLI capability on `main`
