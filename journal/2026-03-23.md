# 2026-03-23

## Release Pipeline (PR #16)
- Migrated `release-orchestrated.yml` to `greenbone/actions/release@v3` with `admin-bypass: "false"`
- Added git auth for private rust-gvm dependency in all CI workflows
- SBOM uploads directly to GH release
- Test release v0.3.0-alpha.7 passed ✅

## CI Fixes (PR #16)
- Removed unused dependencies: `gvm-client`, `gvm-gmp`, `thiserror` (cargo-machete)
- Bumped MSRV from 1.75 → 1.88 (transitive dep `home` 0.5.12 requires 1.88)
- Added `audit.toml` + CI flags to ignore unfixable advisories:
  - RUSTSEC-2026-0048, RUSTSEC-2026-0044 (aws-lc-sys, blocked by russh 0.57.1)
  - RUSTSEC-2023-0071 (rsa Marvin Attack, no upstream fix)
- Added `CARGO_NET_GIT_FETCH_WITH_CLI` + git URL rewrite for private deps in CI

## Team
- Added Michael Wiegand (@wiegandm) as admin
