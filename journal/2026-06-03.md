# 2026-06-03 — CLI Security Hardening Landed, the Auth Path Moved to Typed GMP, and CI Wiring Kept Evolving

## Mainline & Merged Work

### The repo moved from filed security concerns to concrete CLI/auth fixes immediately after the 2026-03-26 baseline
- PR #24 replaced the unsafe terminal password prompt with `rpassword`, and PR #25 followed by hardening CLI auth and input handling; together they closed the security issue opened as #23 and also resolved the previously filed issues #6, #7, #8, #11, #12, #13, and #14
- That makes the biggest change in this window not a new feature but a backlog reset: the security/code-review issue cluster that dominated the 2026-03-26 entry is no longer sitting open on the repo
- PR #27 then migrated the auth path to typed GMP command/response handling, which is the only substantive product-facing code change after the security hardening burst and keeps gvm-rools aligned with the typed API direction elsewhere in the rust-gvm ecosystem

## Release & Documentation Posture
- PR #30 removed obsolete release workflows and added release documentation, which simplifies the release surface compared with the more experimental pipeline state described in late March
- The repo also absorbed journal catch-up commits (#26 and #28) plus a README update (#58), but those were secondary to the security closure and typed-auth migration
- There has been no comparable wave of newly opened issues since #23, so backlog motion here has been mostly about finishing previously identified work rather than discovering new defects

## CI, Dependencies, and Runtime Signals
- CI policy kept moving after the March fixes: PR #59 made the security workflow run on all pull requests, PR #52 refreshed GitHub Actions dependencies, and PR #63 fixed the cross-repo `repository_dispatch` payload shape
- Dependency maintenance also moved `main` forward on 2026-05-07 through PRs #53, #54, and #47, updating the cargo set, `quick-xml`, and `rand`
- Recent health signals are mostly acceptable but not perfectly quiet: the scheduled `Security` run on `main` succeeded on 2026-06-01, while the `Dependabot Updates` dynamic workflow on 2026-06-02 split between success and failure, so the automation baseline is usable but still a little noisy on maintenance lanes
