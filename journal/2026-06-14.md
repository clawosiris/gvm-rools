# 2026-06-14 — The CLI Repo Shifted from Security Cleanup into Maintenance and Workflow Drift, with CI Signal Turning Noisier than the Code Queue

## Mainline & Merged Work

### `main` moved again after the 2026-03-30 baseline, but the work was mostly maintenance and workflow hygiene
- Since the last merged journal entry, `main` absorbed PR #47 (rand bump), PR #52 (actions-group dependency updates), PR #53 (cargo-group dependency updates), PR #54 (quick-xml bump), PR #58 (README update), PR #59 (run security workflow on all pull requests), and PR #63 (repository_dispatch payload fix)
- That sequence matters because the repository did not reopen broad CLI feature work after the March security burst. It shifted into dependency upkeep, documentation touch-ups, and CI wiring corrections
- The repo is therefore not stalled, but its center of gravity has clearly moved away from product behavior and toward keeping surrounding automation from drifting out of spec

## Issues
- No issues were opened after the 2026-03-30 journal baseline
- No issues were closed in the same window either, so the issue tracker did not move with the maintenance work on `main`
- The remaining open issue picture is still anchored by the older review follow-up issue #10 rather than any fresh bug wave

## Pull Request Queue
- The live non-journal queue is thin but uneven: PR #67 (quick-xml bump to 0.40.0), PR #76 (journal PR automation workflow), and PR #95 (actions dependency updates) are the notable open branches
- Journal backlog is much larger than the substantive queue, with PRs #77 through #100 open on daily entry branches
- That means branch volume is misleading here. The actual engineering queue is small; what is large is the unmerged documentation automation trail

## CI Health
- `main` is no longer giving the same clean signal it had at the end of March. The most recent `Dependabot Updates` runs on `main` split between success and failure, and the scheduled `Security` workflow failed on 2026-06-08
- The newest active maintenance branch is also noisy: PR #95 is currently failing both `Cargo Audit` and `Security`, even though its lint/test lanes pass and E2E triggering is skipped as expected
- The older open maintenance branch looks healthier by comparison: PR #67 still shows a fully green check set, which reinforces that the current instability is concentrated in newer workflow/dependency churn rather than across the whole repo
