# 2026-07-08 — Quick-XML Fix Blocked as Incomplete

## Dependency Remediation

### PR #140 No Longer Looks Sufficient
- Open PR [#140](https://github.com/clawosiris/gvm-rools/pull/140) received a `CHANGES_REQUESTED` review on 2026-07-07 explaining that the direct `quick-xml` bump to `0.41.0` still leaves vulnerable `quick-xml 0.39.4` entries in `Cargo.lock` through pinned `rust-gvm` packages
- That makes the current branch a blocked partial fix rather than a likely security-resolution path: `Security` is still failing on the PR, and the actionable next step shifts to updating the pinned upstream dependency chain or otherwise removing the transitive vulnerable copy
- The dependency queue is therefore narrower but more explicit now: this repo does not just need another version bump PR, it needs a remediation path that clears the transitive audit findings called out in the review
