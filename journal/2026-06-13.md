# 2026-06-13 — Typed Auth Finally Landed, Release Plumbing Simplified, and the Repo Drifted into Workflow and Dependency Maintenance

## Security & Product Fixes

### Typed CLI auth path migration (PR #27, merged 2026-03-31)
- The repo finally moved its CLI authentication path onto the typed GMP command/response surface from upstream `rust-gvm`
- That is the most meaningful functional change in this window because it aligns the CLI with the ecosystem's typed-model direction instead of leaving auth on a more bespoke parsing path

## CI/CD Changes

### Release and workflow cleanup continued after the 2026-03-30 baseline
- PR #30 removed obsolete release workflows and added release documentation on 2026-03-31, simplifying the release path rather than expanding it
- PR #59 added `Security` workflow execution on all pull requests, and PR #63 later fixed the `repository_dispatch` payload shape used for cross-repo triggering
- Maintenance churn also landed on `main`: PR #52 bumped the actions group, PR #53 bumped the cargo dependency group, PR #54 moved `quick-xml` forward, and PR #47 bumped `rand`
- Taken together, the repo's movement after March was mostly operational hygiene: workflow correctness, dependency currency, and keeping the cross-repo dispatch path usable

## Issues
- No issues were opened after the 2026-03-30 journal baseline
- No issues were closed in the same window either

## Open Issues
- [#10](https://github.com/clawosiris/gvm-rools/issues/10) remains the only open issue, so the backlog shape has not changed even though `main` did

## CI Health
- The current signal is uneven but understandable. Recent journal PRs consistently pass `CI` while failing `Security`, which points to tooling or policy friction rather than broad breakage
- `Dependabot Updates` on `main` is also mixed, with both successful and failed runs visible in June
- There is not much fresh mainline product verification signal beyond that because the repo's visible work since late May has been maintenance-heavy rather than feature-heavy
