# 2026-05-09 — Mainline Is Still Carrying the Dispatch Bug, Even Though the Remaining Queue Is Green

## Mainline & Merged Work

### `main` absorbed a long backlog after the 2026-03-30 journal baseline, then stopped on a real workflow defect
- The first post-baseline product and maintenance moves landed on 2026-03-31 through PRs #27 (`feat(cli): migrate auth path to typed GMP command/response`) and #30 (`chore: remove obsolete release workflows, add release documentation`), alongside the merged journal PRs #26 and #28
- After a long quiet stretch, `main` moved sharply again on 2026-05-07 by merging PR #59 (`ci: run security workflow on all pull requests`), PR #58 (`Update README.md`), and the maintenance PRs #52, #53, #54, and #47
- No additional commits have landed since that 2026-05-07 merge wave, so the current story is no longer backlog throughput; it is whether the repo fixes the automation bug now exposed on `main`

## Pull Request Queue
- Two journal pull requests are open: #57 (`docs(journal): consolidate pending journal entries through 2026-05-07`) and #61 (`docs(journal): add 2026-05-08 journal entry`)
- PR #57 is approved, clean, and green, while PR #61 is also green but still review-gated
- That queue is not the real blocker anymore; from a branch-health perspective, the remaining PRs are in better shape than the default branch itself

## Issues
- No issues were opened after the 2026-03-30 journal baseline
- No issues were closed in the same window
- The repo's important movement in this period was shipped maintenance and workflow behavior, not backlog churn

## CI Health
- `Security` is behaving correctly on pull requests after PR #59 broadened coverage to all PRs, including docs-only branches
- `main` is still not healthy: the latest push `CI` run on 2026-05-07 failed in `Trigger E2E Tests` while `Security` passed, and the open journal PRs stay green only because that trigger step is skipped there
- The failure remains concrete and reproducible: the workflow sends `client_payload` to `gh api repos/clawosiris/rust-gvm-e2e-tests/dispatches` as a JSON string instead of an object, and GitHub returns HTTP 422 (`... is not an object`)
