# 2026-07-09 — Mainline Landed the CLI Flag Fix and Dependency Refresh

## Mainline Delivery
- `main` moved again after the 2026-07-08 journal cutoff when PR [#144](https://github.com/clawosiris/gvm-rools/pull/144) merged on 2026-07-08 23:46 UTC
- That merge did more than just land the requested CLI parsing fix. It shipped the `gvm-cli` change that accepts global flags after transport subcommands, updated the direct `quick-xml` dependency to `0.41.0`, bumped `gvm-mock-server`, and refreshed the pinned `rust-gvm` chain to carry the security remediation through the lockfile
- This changes the repo posture from “open security remediation debate” to an actual shipped mainline attempt at both the CLI usability fix and the `quick-xml` cleanup path

## Validation
- The immediate post-merge `CI` and `Security` runs on `main` both completed successfully, so the repo’s own validation surface treated PR #144 as a clean delivery instead of another half-fixed dependency branch
- That matters because the previous 2026-07-08 journal entry captured PR #140 as an incomplete path; by the end of the same day the project had moved past that blocked state into a merged replacement fix

## Attention
- One fresh ecosystem signal complicates the otherwise clean merge: a downstream `rust-gvm-e2e-tests` `repository_dispatch` run against `gvm-rools` commit `8bff56d` later failed in the CLI smoke layer with `gvm-cli: command not found`
- So `gvm-rools` itself shipped and stayed green locally, but the wider stack still needs a follow-up check on CLI availability or invocation expectations in the shared E2E harness
