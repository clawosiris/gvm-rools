# 2026-05-08 — Mainline Cleared Most Maintenance Debt, Then Exposed a Real CI Bug

## Mainline & Merged Work

### `main` moved a lot after the 2026-03-30 journal baseline, first with CLI/release work and then with a large maintenance wave
- The first post-baseline product moves landed on 2026-03-31: PR #27 (`feat(cli): migrate auth path to typed GMP command/response`) and PR #30 (`chore: remove obsolete release workflows, add release documentation`), alongside the merged journal PRs #26 and #28
- After a long quiet stretch on `main`, the repo moved sharply again on 2026-05-07 by merging PR #59 (`ci: run security workflow on all pull requests`), PR #58 (`Update README.md`), and four maintenance PRs: #52, #53, #54, and #47
- That 2026-05-07 wave cleared most of the previously open maintenance stack and materially improved branch-protection behavior for docs-only pull requests

## Pull Request Queue
- The only open pull request now is #57 (`docs(journal): consolidate pending journal entries through 2026-05-07`)
- PR #57 is approved, merge-clean, and green, so it is not the thing holding the repo back anymore
- The queue story has changed from “too many waiting branches” to “almost everything merged, but `main` itself is now unhealthy”

## Issues
- No issues were opened after the 2026-03-30 journal baseline
- No issues were closed in the same window
- That means the important movement here was delivery and automation hygiene rather than backlog triage

## CI Health
- The `Security` workflow is now behaving correctly on pull requests after PR #59 removed the docs-only blind spot and added a final required check path
- However, `main` CI is not actually healthy yet: multiple `CI` runs on 2026-05-07 failed in the `Trigger E2E Tests` step, including the latest run for merged PR #47
- The failure is reproducible and specific: the workflow calls `gh api repos/clawosiris/rust-gvm-e2e-tests/dispatches -f event_type=component-updated -f 'client_payload={"component":"gvm-rools","ref":"<sha>"}'`, and GitHub rejects it with HTTP 422 because `client_payload` is being sent as a JSON string instead of an object
