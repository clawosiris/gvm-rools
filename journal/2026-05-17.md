# 2026-05-17 — Mainline Absorbed Typed Auth Work, CI/Dependency Maintenance, and a Backlog Cleanup, but the Queue Is Still Waiting on Review

## Mainline & Merged Work

### `main` moved substantially after the 2026-03-30 journal baseline, and the change was broader than a simple docs catch-up
- On 2026-03-31, `main` absorbed PR #27 (`feat(cli): migrate auth path to typed GMP command/response`) and PR #30 (`chore: remove obsolete release workflows, add release documentation`), which means the repo did real product and release-pipeline work immediately after the last committed journal entry
- A larger maintenance burst landed on 2026-05-07: PR #47 (`build(deps): bump rand from 0.8.5 to 0.8.6`), PR #52 (`ci(deps): bump the actions group across 1 directory with 2 updates`), PR #53 (`build(deps): bump the cargo group with 4 updates`), PR #54 (`build(deps): bump quick-xml from 0.37.5 to 0.39.2`), PR #58 (`Update README.md`), and PR #59 (`ci: run security workflow on all pull requests`) all merged
- Taken together, that is a meaningful default-branch stretch: typed auth handling changed, old release automation was removed, dependency maintenance landed, repository documentation was refreshed, and the security workflow was expanded to cover pull requests

## Pull Request Queue

### The current queue is healthy on checks, but it is still mostly waiting for someone to press merge
- PR #63 (`fix(ci): send repository_dispatch payload as object`) opened on 2026-05-09 and remains the most important unmerged branch because it directly targets CI behavior and is clean on checks
- PR #66 (`build(deps): bump the cargo group with 2 updates`) and PR #67 (`build(deps): bump quick-xml from 0.39.2 to 0.40.0`) also remain open and clean, so the dependency lane is lined up behind the CI fix rather than blocked by automation
- Journal PRs #61, #62, #64, #65, #68, #69, #70, #71, and #72 are still open, while older journal PRs #29, #31 through #46, #48 through #51, #55, and #56 were closed without merge as backlog cleanup happened around 2026-04-30 and 2026-05-07

## Issues
- No issues were opened after the 2026-03-30 journal baseline
- No issues were closed in the same window
- That means the repo's visible activity has been almost entirely code/CI maintenance and queue management rather than new bug intake

## CI Health
- `Security` on `main` has stayed green more recently than `CI`, including a successful scheduled run on 2026-05-11 and successful push-triggered runs on 2026-05-07
- The caution flag is the default-branch `CI` signal: push runs on 2026-05-07 failed, and there is no newer successful `CI` run on `main` afterward
- So this repo is healthier than it was at the 2026-03-30 baseline, but it still needs review throughput and probably an explicit `main` CI refresh after #63 lands
