# 2026-05-18 — Mainline Finally Moved Again with Typed Auth, Release Cleanup, and a May Merge Burst, but the Queue and CI Story Are Still Uneven

## Mainline & Merged Work

### `main` changed substantially after the 2026-03-30 journal baseline, with one late-March implementation wave and one early-May maintenance wave
- `main` picked up four commits on 2026-03-31: merged journal PRs #28 and #26, PR #27 (`feat(cli): migrate auth path to typed GMP command/response`), and PR #30 (`chore: remove obsolete release workflows, add release documentation`)
- That late-March wave mattered: the CLI auth path moved onto the typed GMP command/response path, and the release workflow surface was deliberately simplified and documented
- After a long quiet stretch, `main` moved again on 2026-05-07 with six more commits: PR #59 (`ci: run security workflow on all pull requests`), PR #58 (`Update README.md`), PR #52 (`ci(deps): bump the actions group across 1 directory with 2 updates`), PR #53 (`build(deps): bump the cargo group with 4 updates`), PR #54 (`build(deps): bump quick-xml from 0.37.5 to 0.39.2`), and PR #47 (`build(deps): bump rand from 0.8.5 to 0.8.6`)
- So unlike the other repos in this pass, `gvm-rools` really did absorb meaningful default-branch change: workflow behavior was tightened, docs were refreshed, and several dependency updates landed cleanly on top of the older security-hardening baseline

## Pull Request Queue

### The queue is still dominated by journal backlog, but one CI fix branch stands out as the real operational item
- A long run of standalone journal PRs was opened after the baseline, and many of the older ones were later closed unmerged in consolidation sweeps rather than merged individually
- The current open docs backlog now starts at PR #57 (`docs(journal): consolidate pending journal entries through 2026-05-07`) and continues through #61, #62, #64, #65, #68, #69, #70, #71, #72, and #73, which means journal maintenance is still piling up faster than it is being absorbed into `main`
- The most important non-doc branch is still PR #63 (`fix(ci): send repository_dispatch payload as object`), opened on 2026-05-09, and it remains clean across the active check surface
- Dependabot also added two more clean maintenance branches on 2026-05-11: PR #66 (`build(deps): bump the cargo group with 2 updates`) and PR #67 (`build(deps): bump quick-xml from 0.39.2 to 0.40.0`)
- One branch was explicitly dropped instead of repaired: PR #60 (`build(deps): bump rpassword from 7.4.0 to 7.5.0`) was closed unmerged on 2026-05-07

## Issues
- No issues were opened after the 2026-03-30 journal baseline
- No issues were closed in the same window either
- This repo's story is therefore not about issue backlog movement; it is about merge throughput, workflow correctness, and how much journal/maintenance debt is being allowed to accumulate

## CI Health
- `main` has kept producing successful `Security` runs through 2026-05-11, including scheduled successes on 2026-04-27, 2026-05-04, and 2026-05-11 plus push-triggered successes when the 2026-05-07 merge burst landed
- The caution flag is the `CI` workflow on `main`: every push-triggered `CI` run tied to the 2026-05-07 merge burst finished in failure even while the paired `Security` run succeeded
- Dependabot-generated update runs on `main` are mixed rather than reassuring: some cargo update lanes succeeded, while GitHub Actions, pip, and later cargo updates also produced failures or cancellations
- So the repo is healthier than it was in late March because real work merged, but it is still not clean operationally; the next sensible move is to land PR #63 and use that plus a fresh `main` validation cycle to see whether the stale/failing default-branch CI picture improves
