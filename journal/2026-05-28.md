# 2026-05-28 — `main` Finally Moved Beyond the March Security Snapshot, Mostly Through CI Hardening and Dependency Catch-Up

## Mainline & Merged Work

### The default branch stayed quiet for weeks after the 2026-03-30 baseline, then resumed with one product-facing auth migration, a May maintenance burst, and an E2E dispatch fix
- The first post-baseline substantive merge was PR #27 (`feat(cli): migrate auth path to typed GMP command/response`) on 2026-03-31, which continued the move toward the typed GMP machinery already landing across the wider rust-gvm ecosystem
- That same day, PR #30 removed obsolete release workflows and added release documentation, so the repo simplified its release path immediately after the earlier security/CI cleanup entry
- The next real wave did not arrive until 2026-05-07, when PR #59 expanded the `Security` workflow to all pull requests, PR #58 refreshed the README, and dependency PRs #47, #52, #53, and #54 all merged in rapid succession
- The newest default-branch change is PR #63 (`fix(ci): send repository_dispatch payload as object`) on 2026-05-25, which matters because it repairs the cross-repo signal path into `rust-gvm-e2e-tests` instead of just changing local polish

## Issues
- No new issues were opened after the 2026-03-30 journal entry
- No issues were closed in the same window either
- So this repo's story since the last entry is entirely about merged code and CI/process maintenance, not issue-tracker churn

## Pull Request Queue
- The current queue is dominated by backlog rather than fresh feature work: journal PRs #77 through #85 are still open, with the newest visible lane at #85 for 2026-05-26
- The only non-journal open branches are maintenance follow-ons, including dependency PR #67 plus the newer blocked updates #75 and #84
- That leaves the repository in a familiar state: `main` did take useful maintenance fixes, but branch cleanup is still lagging behind

## CI Health
- The strongest current signal is the newest default-branch push: PR #63 completed both `CI` and `Security` successfully on 2026-05-25
- Scheduled `Security` automation on `main` also succeeded on 2026-05-25, so the hardened PR coverage from #59 did not immediately produce new branch-wide instability
- The remaining noise is isolated to update automation: dynamic runs involving `github/codeql-action` failed while the cargo and other GitHub Actions update lanes succeeded, which suggests maintenance churn rather than broad pipeline breakage
