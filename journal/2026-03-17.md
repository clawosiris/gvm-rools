# 2026-03-17 — gvm-rools Kickoff (gvm-cli focus)

## Prompts & Actions Log

### Prompt
- "Analyze the Greenbone gvm-tools repo. Create a new repo named gvm-rools for a Rust-reimplementation of gvm-tools on top of rust-gvm... Focus on gvm-cli"

### Actions Taken
- Cloned and reviewed `greenbone/gvm-tools`:
  - Identified primary CLI tools: `gvm-cli`, `gvm-script`, `gvm-pyshell`
  - Identified `gvm-cli` behavior: send raw GMP/OSP XML commands, supports unix socket/TLS/SSH transports, optional auth, raw vs checked output, pretty print, duration measurement
- Created GitHub repo: `clawosiris/gvm-rools` (private)
- Bootstrapped Rust workspace:
  - Created crate: `crates/gvm-rools-cli`
  - Wired dependencies to rust-gvm workspace crates (`gvm-connection`, `gvm-protocol`, etc.), pinned to `rust-gvm` tag `v0.2.0`
  - Implemented initial `gvm-cli` skeleton:
    - Transports: unix socket + SSH (TLS stub)
    - Flags: `--xml` / infile / stdin, `--raw`, `--pretty` (warn: not implemented), `--duration`, `--gmp-username`, `--gmp-password` (prompting TODO)
    - Behavior: connect → optional authenticate → send → read → print; non-2xx exits 1 unless `--raw`

### Prompt
- "Please add the same people from rust-gvm to gvm-rools"

### Actions Taken
- Added collaborators to `gvm-rools` matching `rust-gvm` roles:
  - Admin: `llunved`, `recepkizilarslan`, `daniel-grnbn`
  - Write: `g-w`, `ts-AIBIoT`, `jsch-gb`

### Prompt
- "Please also add @benjaminhoener to rust-gvm and gvm-rools"

### Actions Taken
- Invited `benjaminhoener` to both `rust-gvm` and `gvm-rools` with **write** access

## Open TODOs
- Add `README.md`, `LICENSE` (AGPL), `SECURITY.md`, `docs/STATUS.md` and CI workflows matching rust-gvm best practices
- Add OpenSpec for `gvm-cli` (scope/CLI surface/transport matrix/test plan)
- Add CLI integration tests using `gvm-mock-server`
- Add SBOM generation + post-processing + sbomqs quality gate
- Add password prompt support (`rpassword`)
