# 2026-05-16 — Typed Auth Migration Landed First, Then an Early-May Wave Brought Dependency Refreshes, README Cleanup, and Broader CI Security Coverage

## Product & Maintenance Changes

### The first post-baseline move on `main` was substantial application work, not just maintenance churn
- On 2026-03-31, `main` picked up PR #27 (`feat(cli): migrate auth path to typed GMP command/response`) and PR #30 (`chore: remove obsolete release workflows, add release documentation`)
- Those changes materially advanced the codebase beyond the 2026-03-30 journal baseline: the CLI auth path moved onto typed GMP command/response handling, and the release story was cleaned up and documented
- The paired journal merges (#26 and #28) also landed the then-pending narrative backlog on `main`

### A second, denser wave landed on 2026-05-07 and was mostly maintenance plus repo hygiene
- `main` then picked up PR #47 (`build(deps): bump rand from 0.8.5 to 0.8.6`), PR #52 (`ci(deps): bump the actions group across 1 directory with 2 updates`), PR #53 (`build(deps): bump the cargo group with 4 updates`), PR #54 (`build(deps): bump quick-xml from 0.37.5 to 0.39.2`), PR #58 (`Update README.md`), and PR #59 (`ci: run security workflow on all pull requests`)
- That is a meaningful default-branch batch: dependency surface moved forward, repository documentation was refreshed, and the security workflow now covers all pull requests instead of leaving that validation narrower than it should be
- PR #60 (`build(deps): bump rpassword from 7.4.0 to 7.5.0`) was closed unmerged the same day, so not every maintenance branch made it through the funnel

## CI/CD Changes

### CI policy got stronger even though the latest `main` push signal is not perfectly clean
- PR #59 broadened `Security` coverage to all pull requests, which is the most important workflow-policy change in the window
- Open PRs #63, #66, and #67 are all approved, clean, and fully green, so the current queue looks healthier than the latest default-branch push runs
- The wrinkle is that the most recent visible `CI` push runs on `main` from 2026-05-07 failed while the corresponding `Security` push runs succeeded, and later `Dependabot Updates` runs on 2026-05-11 failed as well

## Issues
- No issues were opened after the 2026-03-30 journal baseline
- No issues were closed in the same window
- This repo's movement was almost entirely through merged PRs and CI/workflow changes rather than through issue churn

## Open Pull Request Queue
- PR #63 (`fix(ci): send repository_dispatch payload as object`) is approved, clean, and still the clearest operational fix waiting to land
- PR #66 (`build(deps): bump the cargo group with 2 updates`) and PR #67 (`build(deps): bump quick-xml from 0.39.2 to 0.40.0`) are also approved and green, so there is low-risk maintenance work ready for merge behind the CI-fix branch
- Journal PRs #61, #62, #64, #65, #68, #69, #70, and #71 show how much docs backlog has accumulated since the last journal file actually made it onto `main`

## CI Health
- `main` is not giving a perfectly boring signal right now: `Security` is green, but the latest visible `CI` push runs from the 2026-05-07 merge wave failed
- Branch-level health is better than branch-wide health: the important open PRs are all green, approved, and structurally easy to merge
- So the repo looks mergeable, but it would be smart to refresh or inspect `main` CI after the next merge instead of assuming the default branch is already clean
