# 2026-06-07 — `main` Shifted from Security Hardening into Maintenance Bursts and Cross-Repo Dispatch Repair

## Product & Maintenance Changes

### Typed auth migration and release-workflow cleanup landed immediately after the 2026-03-30 baseline
- PR #27 migrated the CLI auth path onto typed GMP commands and responses on 2026-03-31, carrying the earlier security-hardening lane into the actual request/response surface
- PR #30 removed obsolete release workflows and added release documentation the same day, reducing workflow sprawl instead of continuing to accumulate parallel release paths

### A concentrated May maintenance burst followed
- On 2026-05-07, `main` absorbed PR #47 (`rand` bump), PR #52 (GitHub Actions dependency updates), PR #53 (cargo dependency group updates), PR #54 (`quick-xml` bump), PR #58 (README refresh), and PR #59 (run `Security` on all pull requests)
- That burst reads as repository maintenance and policy tightening, not new CLI feature expansion

### Cross-repo E2E dispatch repair landed on 2026-05-25
- PR #63 fixed `repository_dispatch` payload handling by sending the payload as an object instead of a malformed shape
- That is a small change locally, but it is operationally important because `gvm-rools` uses that dispatch path to trigger work in `rust-gvm-e2e-tests`

## Issues
- No new issues opened after the 2026-03-30 baseline
- No issues closed in the same window either, so the issue tracker stayed mostly static while maintenance work continued to land

## Open Issues
- [#10](https://github.com/clawosiris/gvm-rools/issues/10) remains the long-lived code-review follow-up bucket

## CI Health
- `main` still has a usable baseline: the scheduled `Security` run succeeded on 2026-06-01
- Dependabot update traffic on 2026-06-02 was mixed rather than clean, with cargo update lanes succeeding while GitHub Actions and `pip` update lanes failed
- The most persistent fresh red signal is now on journal PRs rather than `main`: from 2026-06-03 through 2026-06-06, docs-only journal branches repeatedly passed `CI` but failed `Security`, which helps explain why the journal backlog has stayed open
