# 2026-07-17 — The Actions Refresh Picked Up Blocking Review Findings

## Review Movement
- Dependabot PR [#131](https://github.com/clawosiris/gvm-rools/pull/131), which updates the repository's GitHub Actions pins, received a changes-requested review after the 2026-07-09 journal entry.
- The proposed pins leave several inline version comments stale: the `actions/checkout` pin has moved to v7 while comments still identify v6, and `github/codeql-action/upload-sarif` has moved to v4 while its comment still identifies v3.

## Security Attention
- The PR's branch remains red in `Cargo Audit` and the aggregate `Security` check because it still carries `quick-xml 0.39.4`, affected by high-severity advisories `RUSTSEC-2026-0194` and `RUSTSEC-2026-0195`; the reviewed remediation is an upgrade to at least `0.41.0`.
- PR #131 remains open and mergeable but is not merge-ready until the pin annotations and stale dependency state are refreshed, despite `main` having already moved through the separate PR #144 delivery.
