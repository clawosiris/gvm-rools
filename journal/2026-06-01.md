# 2026-06-01 — The Security Cleanup Wave Closed, Then `main` Shifted into Release-Pipeline Simplification and Maintenance CI Work

## Product Surface

### The first post-2026-03-26 movement was still product-relevant, because it finished the authentication hardening lane instead of just polishing docs
- PR #24 replaced the unsafe password prompt path with `rpassword`, and PR #25 followed immediately with broader CLI auth/input hardening.
- PR #27 then migrated the auth path onto typed GMP commands and responses, which is the biggest architectural code move in the window because it shifts one of the riskiest CLI paths onto the typed stack.

## Security & Issues
- The repo opened one new issue after the 2026-03-26 baseline: #23 (`security(cli): replace unsafe terminal password handling with rpassword`), and closed it the same day via PR #24.
- More importantly, the backlog described in the 2026-03-26 entry actually burned down: issues #6, #7, #8, #9, #11, #12, #13, and #14 all closed on 2026-03-27 as the hardening work merged.
- That means the prior journal’s nine-issue security overhang is no longer the active story for this repo; the remaining work shifted toward typed-client migration, release-process cleanup, and routine maintenance.

## CI/CD Changes
- PR #22 added Semgrep SAST plus CI/Security badges, tightening the repo’s visible security posture right as the hardening fixes landed.
- PR #19 removed the old standalone release workflow, and PR #30 later removed the remaining obsolete release workflows while adding release documentation.
- In May, the pipeline focus moved from structural cleanup to upkeep: PR #52 bumped the Actions group, PR #59 required the Security workflow on all pull requests, and PR #63 fixed the `repository_dispatch` payload shape for cross-repo triggers.
- The same May burst also brought normal dependency churn onto `main` through PRs #47, #53, and #54, plus a README update in PR #58.

## CI Health
- The latest `main` signal is mostly green, but not perfectly so. Cargo and most maintenance lanes passed on 2026-05-25 and 2026-05-29.
- The weak spot is still workflow-dependency churn: the `github/codeql-action` Dependabot lane failed twice on 2026-05-25 even as the surrounding cargo and top-level actions updates succeeded.
- That leaves `gvm-rools` in a better place than the 2026-03-26 baseline: the security backlog is largely resolved, dispatch wiring is corrected, and the remaining CI noise is concentrated in a specific maintenance lane instead of the core product path.
