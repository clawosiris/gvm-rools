# 2026-05-13 — Mainline Finally Moved in Two Waves, but the Important CI Dispatch Fix Is Still Waiting to Land

## Mainline & Merged Work

### `main` absorbed a real tranche of work after the 2026-03-30 journal baseline
- `main` picked up ten commits in the window, starting on 2026-03-31 with PR #27 (`feat(cli): migrate auth path to typed GMP command/response`) and PR #30 (`chore: remove obsolete release workflows, add release documentation`), plus the merged journal consolidations #26 and #28
- The second and more visible wave landed on 2026-05-07: PR #47 (`build(deps): bump rand from 0.8.5 to 0.8.6`), PR #52 (`ci(deps): bump the actions group across 1 directory with 2 updates`), PR #53 (`build(deps): bump the cargo group with 4 updates`), PR #54 (`build(deps): bump quick-xml from 0.37.5 to 0.39.2`), PR #58 (`Update README.md`), and PR #59 (`ci: run security workflow on all pull requests`)
- That mix matters because the repo did more than drift on maintenance: it refreshed the typed GMP auth path, cleaned up release workflow/documentation, and improved security signal quality for future pull requests

## Issues
- No issues were opened after the 2026-03-30 journal baseline
- No issues were closed in the same window
- The backlog has moved through pull requests and docs churn rather than through issue triage

## Pull Request Queue
- PR #63 (`fix(ci): send repository_dispatch payload as object`) is still the most important unmerged implementation branch; it remains green across `Clippy`, `Format`, `MSRV Check`, `Test (stable)`, `Cargo Audit`, `Security`, and `Semgrep SAST`
- PRs #66 (`build(deps): bump the cargo group with 2 updates`) and #67 (`build(deps): bump quick-xml from 0.39.2 to 0.40.0`) are also fully green, so maintenance is not blocked by failing automation
- Journal backlog PRs #57 and #61 through #68 continue to stack up, but they are secondary to landing #63 because the repository still lacks default-branch proof that the repository-dispatch payload fix actually shipped

## CI Health
- `Security` on `main` stayed healthy across the visible window, including successful push runs on 2026-05-07 and a successful scheduled run on 2026-05-11
- `CI` on `main` was noisier during the 2026-05-07 merge wave, with several push runs failing even as the paired `Security` runs succeeded, so the repo's automation picture is mixed rather than clean
- The best branch-level news is that #63, #66, and #67 are all green today; the missing step is human throughput, not machine validation
