# 2026-05-26 — Mainline Finally Picked Up the CI Dispatch Fix While the Maintenance Queue Kept Sprawling

## Mainline & Merged Work

### `main` changed in two distinct waves after the 2026-03-30 journal baseline, and the newest one finally repaired cross-repo workflow dispatch
- The first wave landed on 2026-03-31 and 2026-05-07: PR #27 (`feat(cli): migrate auth path to typed GMP command/response`), PR #30 (release-workflow cleanup and release docs), PR #52 (actions updates), PR #53 (cargo group updates), PR #54 (`quick-xml`), PR #58 (README refresh), and PR #59 (`ci: run security workflow on all pull requests`)
- The second wave was much narrower but more operationally important: PR #63 (`fix(ci): send repository_dispatch payload as object`) merged on 2026-05-25
- So the repo finally converted an aging CI fix branch into `main`, which matters more than another round of maintenance churn because it restores the contract used to trigger downstream workflows

## Issues
- No issues were opened after the 2026-03-30 journal entry
- No issues were closed in the same window
- Like the last time this repo moved, the story remains overwhelmingly PR-driven rather than issue-driven

## Pull Request Queue
- The new open maintenance branch is PR #84 (`build(deps): bump the cargo group across 1 directory with 3 updates`), opened on 2026-05-25
- Older queue items remain in place: PR #67 (`quick-xml`), PR #75 (CodeQL action bump), and PR #76 (journal PR automation) are all still open, with journal PRs #77 through #83 stacked on top
- That leaves the repository looking active but not especially efficient: `main` is healthier than before, yet the review queue is still dominated by unattended maintenance and journal branches

## CI Health
- The newly merged CI dispatch fix validated cleanly on 2026-05-25 with successful `CI` and `Security` runs on `main`
- Scheduled `Security` also passed the same day, so the core protection lane is stable
- The weak spot is maintenance automation: recent `Dependabot Updates` runs are mixed, including repeated failures in the CodeQL action update lane even while the cargo-group updates succeed
