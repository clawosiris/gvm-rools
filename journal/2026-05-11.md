# 2026-05-11 — Typed Auth Migration and CI Cleanup Landed, but Default-Branch CI Still Needs the Dispatch Fix

## Mainline & Merged Work

### `main` absorbed a long backlog after the 2026-03-30 journal baseline
- `main` picked up eleven in-scope commits across two distinct waves. On 2026-03-31, the repo merged PR #27 (`feat(cli): migrate auth path to typed GMP command/response`), PR #30 (`chore: remove obsolete release workflows, add release documentation`), and journal PRs #26 and #28. On 2026-05-07, it merged dependency updates #47, #52, #53, and #54, docs refresh #58 (`Update README.md`), and CI policy change #59 (`ci: run security workflow on all pull requests`)
- The most meaningful product change in the whole window is still PR #27, which moved the CLI auth path onto the typed GMP command/response flow
- The most meaningful automation change is PR #59, because it improves branch-protection signal quality for future pull requests in the same way similar rollouts did elsewhere in the ecosystem

## Pull Request Queue
- The repo’s only truly important open implementation lane is PR #63 (`fix(ci): send repository_dispatch payload as object`)
- PR #63 is technically healthy: `Format`, `Cargo Audit`, `Clippy`, `Unused Dependencies`, `Test (stable)`, `MSRV Check`, `Security`, and `Semgrep` are all green, while `Trigger E2E Tests` is skipped on the PR branch as expected
- Behind it, the queue is mostly journal backlog again: PRs #61, #62, and #64 are open and green, but they matter far less than getting the dispatch fix merged

## Issues
- No issues were opened after the 2026-03-30 journal baseline
- No issues were closed in the same window
- That makes this repo unusual for the period: the work moved through pull requests and maintenance batches rather than through visible issue churn

## CI Health
- Default-branch health is still the uncomfortable part. The 2026-05-07 `Security` push runs succeeded, but the latest `CI` push runs on `main` failed after the dependency wave landed
- The presence of green checks on PR #63 strongly suggests the repo already has the fix in hand; `main` just has not absorbed it yet
- So the repo’s current state is “repair branch proven, default branch still red,” and the next sensible step is still to merge #63 and verify the next `main` run actually recovers the repository-dispatch path
