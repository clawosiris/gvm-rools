# 2026-07-05 — `main` Took a Small Dependency Bump While the Maintenance Queue Turned into a Security-Failure Stack

## Mainline & Merged Work

### This window finally moved `main`, but only through a small dependency update
- PR #130 merged on 2026-07-05 and bumped `anyhow` from `1.0.102` to `1.0.103`.
- That is the only new default-branch commit after the 2026-07-03 local baseline, so the shipped CLI surface still did not change in a feature sense.
- The more interesting implication is process-level: unlike the previous entry's completely frozen merge lane, this repo did manage to land one maintenance update instead of remaining fully stuck.

## Issues
- One new issue opened in the same window: #136 proposes typed operational subcommands while keeping the raw-XML `gvm-cli` mode.
- No issues closed after the previous local journal entry, so the tracker expanded slightly instead of shrinking.
- That leaves the issue story modest but real: feature ideas are still arriving even while the repo is spending most of its visible energy on dependency maintenance.

## Pull Request Queue
- The queue kept moving even though only one PR merged. PR #137 opened to consolidate the `rust-gvm` lockfile refresh, while the older maintenance and journal branches are still waiting: PR #131 for the actions-group update, PR #104 for the `quick-xml` bump, PR #116 for the release-asset upload fix, and PRs #132 through #135 for the unmerged journal backlog.
- So the queue is not just large; it is now layered around one new merged maintenance update, one fresh lockfile-follow-up branch, older dependency refreshes, and a still-growing journal pile.

## CI Health
- The validation story split hard in this window. `CI` was green across the merged `anyhow` bump, the fresh lockfile refresh PR, and the visible dependency-update PRs.
- `Security` told the opposite story: the 2026-07-05 push to `main` failed, and the matching PR security runs for the `quick-xml`, `rust-gvm` crate bumps, actions-group update, and lockfile refresh all failed as well.
- The clearest failure mode is now explicit in `cargo audit`: `quick-xml` `0.39.4` is still present in the graph and is flagged for `RUSTSEC-2026-0194` and `RUSTSEC-2026-0195`, both fixed by upgrading to `>=0.41.0`. So this repo is no longer just carrying generic automation noise; it is carrying a repeatable, specific security backlog that the open maintenance queue has not cleared yet.
