# 2026-07-06 — `main` Resumed Dependency Shipping, the Queue Consolidated Around One Lockfile Refresh, and `Security` Regressed on the New Head

## Mainline & Merged Work

### The post-2026-06-18 window finally added one real `main` commit after the release burst
- PR #130 merged on 2026-07-05 to bump `anyhow` from 1.0.102 to 1.0.103 in the cargo group, producing the first non-journal default-branch commit since the 0.3.3 release follow-through.
- That is a small change in scope, but it matters because it shows `main` resumed shipping maintenance work instead of staying stuck behind the increasingly stale dependency queue described in the last published entry.

## Issues
- A new issue changed the repo's medium-term direction more than the dependency merge itself. Issue #136 opened on 2026-07-05 to propose typed operational subcommands while keeping the raw XML `gvm-cli` mode.
- That is a materially different conversation from pure maintenance. It reopens the question of whether this repo stays a thin XML client or grows higher-level typed operational workflows on top of the newer `rust-gvm` surface.

## Pull Request Queue
- The dependency queue was partially consolidated rather than merely growing. PRs #122 through #125, which had each tracked separate `rust-gvm` component bumps, were all closed on 2026-07-05.
- A single follow-on PR, #137, opened immediately to consolidate the `rust-gvm` lockfile refresh instead of leaving four parallel stale branches in place.
- That does not remove the maintenance backlog, but it does make the remaining update lane easier to reason about than the blocked multi-PR shape described in the older journal state.

## CI Health
- `CI` passed on the new `main` head after PR #130 merged, so the repo did not lose basic buildability when it resumed shipping maintenance.
- `Security` did regress on that same push, though: the 2026-07-05 `main` run failed after the last published checkpoint had ended on a green security signal.
- That failure is the newest actionable repo-health change. Until it returns to green, the repo's current posture is "shipping again, but not fully healthy" rather than the cleaner maintenance state it briefly held at the end of June.
