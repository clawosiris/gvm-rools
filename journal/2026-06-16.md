# 2026-06-16 — Auth Migration Finally Landed, CI Wiring Changed Repeatedly, and the Security Signal Is Now the Main Operational Problem

## Security Fixes

### The biggest post-2026-03-30 functional merge was the delayed auth-path migration, followed by broader security verification coverage
- `main` absorbed PR #27 on 2026-03-31, migrating the CLI auth path to typed GMP command and response handling one day after the previous journal baseline
- Weeks later, PR #59 moved the `Security` workflow onto all pull requests instead of leaving it as a narrower branch signal
- The net effect is that the repo's post-March security work has centered more on typed-command alignment and broader verification coverage than on new user-facing feature expansion

## CI/CD Changes

### Most subsequent mainline movement was tooling churn rather than product evolution
- `main` also absorbed PR #30 on 2026-03-31 to remove obsolete release workflows and add release documentation, then a May 7 maintenance cluster: PR #52 (actions bump), #53 (cargo group bump), #54 (quick-xml bump), and #58 (README refresh)
- The latest operational fix was PR #63 on 2026-05-25, which corrected the `repository_dispatch` payload shape for downstream automation
- Since then, the open queue has stayed dominated by maintenance and journal backlog rather than new feature work: dependency PRs #95, #103, and #104 sit alongside journal PRs #77 through #102 and the older journal-automation bootstrap branch #76

## Issues
- No issues were opened after the 2026-03-30 journal baseline
- No issues were closed in the same window
- That means the repository's movement has been operational rather than bug-driven for more than two months

## Open Issues
- [#10](https://github.com/clawosiris/gvm-rools/issues/10) — Code review follow-ups (12 items)

## CI Health
- The ordinary CI lane is mostly usable: dependency PRs #103 and #104 both pass `Clippy`, `Format`, `MSRV Check`, and `Test (stable)`, and the latest `repository_dispatch` fix merged cleanly to `main`
- The security signal is the problem now, not compilation or test execution. Scheduled `Security` on `main` failed on 2026-06-15, dependency PRs #95, #103, and #104 all fail `Cargo Audit` and `Security`, and even the journal PRs continue to pass `CI` while failing `Security`
- In other words, this repo no longer looks blocked by basic build breakage; it looks blocked by unresolved audit and security-policy debt
