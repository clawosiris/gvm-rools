# 2026-05-21 — Mainline Quietly Absorbed Typed Auth, Release Cleanup, Multiple Dependency Bumps, and Security Workflow Hardening

## Mainline & Merged Work

### `main` moved substantially after the 2026-03-30 baseline, first with real product/CI work and later with a maintenance burst
- On 2026-03-31, `main` absorbed PR #27 (`feat(cli): migrate auth path to typed GMP command/response`) and PR #30 (`chore: remove obsolete release workflows, add release documentation`), alongside journal PRs #26 and #28.
- On 2026-05-07, the repo took a second merge wave: PR #47 (`rand` 0.8.6), PR #52 (GitHub Actions updates), PR #53 (cargo-group dependency updates), PR #54 (`quick-xml` 0.39.2), PR #58 (`Update README.md`), and PR #59 (`ci: run security workflow on all pull requests`).
- That is a meaningful amount of real movement for a repo whose last merged journal file was still dated 2026-03-30: typed CLI auth landed, obsolete release plumbing was removed, dependency maintenance shipped, README guidance changed, and the security workflow became stricter on pull requests.

## Issues
- No new issues were opened after the 2026-03-30 baseline, and no issue closures were recorded in the same window.
- The story here is therefore execution and maintenance throughput, not new bug intake.

## Open PR Queue
- The oldest useful work is already merge-ready: PR #63 (`fix(ci): send repository_dispatch payload as object`) plus approved dependency PRs #66 (`cargo` group) and #67 (`quick-xml` 0.40.0) are all `CLEAN`.
- Newer maintenance keeps stacking on top: PR #75 (CodeQL action), PR #76 (journal automation), and journal PRs #77/#78 are green but review-blocked, while older journal PR #74 is still parked on `journal-main`.
- So even after two merge waves, the queue is drifting back toward the same pattern as the other repos: plenty of clean work, not enough merge throughput.

## CI Health
- Recent signal remains strong. The latest `CI` and `Security` runs for journal PR #78 passed on 2026-05-20, and the same green pattern appeared for the automation/bootstrap branches on 2026-05-18.
- The clean open branches (#63/#66/#67/#75/#76/#77/#78) reinforce that this repository's normal maintenance path is healthy.
- Operationally this is still the least noisy repo of the four; its problem is backlog aging, not red trunk health.
