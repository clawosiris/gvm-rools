# 2026-05-10 — Dependency Merge Wave, PR-Wide Security Checks, and a Default-Branch CI Recovery Branch Waiting on Review

## Security & CI Changes

### The repo materially tightened its PR safety net after the 2026-03-30 baseline
- PR #59 (`ci: run security workflow on all pull requests`) merged on 2026-05-07, extending the `Security` workflow to all PRs instead of leaving docs-only branches under weaker protection
- That change is already visible in practice: journal PRs #57, #61, #62 and CI-fix PR #63 all received successful `Security` runs
- PR #63 (`fix(ci): send repository_dispatch payload as object`) is now open and green, but it still needs review before the default branch can inherit the dispatch fix

## Mainline & Merged Work

### `main` moved substantially after the 2026-03-30 journal baseline
- `main` picked up eleven commits in the window: typed-auth migration PR #27 and release-workflow cleanup PR #30 on 2026-03-31, followed by a larger 2026-05-07 merge wave for PRs #47, #52, #53, #54, #58, and #59 plus merged journal PR #28
- The May merge wave was mostly maintenance and repository hygiene: dependency bumps for `rand`, `quick-xml`, and the grouped cargo/actions updates, a small README refresh, and the security-workflow expansion
- Unlike the 2026-03-30 entry, this repo's recent story is less about one focused security hardening pass and more about catching up accumulated maintenance while repairing workflow behavior around the edges

## Issues
- No issues were opened after the 2026-03-30 journal baseline
- No issues were closed in the same window
- The absence of issue churn reinforces that most of the repo's recent movement happened through maintenance PRs and workflow repair rather than through tracked product/backlog items

## Open Pull Requests
- Journal PR #57 (`docs(journal): consolidate pending journal entries through 2026-05-07`) is still open even though it is already `APPROVED`, so the repo still has documentation backlog on top of the newer #61 and #62 journal PRs
- PR #63 is the most important active branch: it is green on both `CI` and `Security`, but it remains `BLOCKED` on review, which means `main` has not yet inherited the repository-dispatch payload fix
- The current queue is therefore not a code-generation problem; it is review sequencing across one CI-recovery branch and several still-open journal branches

## CI Health
- Default-branch health is still mixed: the `Security` workflow succeeded on recent `main` pushes, but push-triggered `CI` failed on the 2026-05-07 merges for PRs #54 and #47
- The green PR checks on #63 strongly suggest the repair path is already in hand, but the fix has not landed yet, so `main` remains behind the healthy branch state
- Until #63 merges, the repo's best execution signal is on the PR branch rather than on the default branch itself
